<!DOCTYPE html>
<html>
  <head>
    <title>RailsSimpleAirbnb</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.1.2/css/all.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
  <div class="" data-controller="show-form">
  <div class="navbar">
  <h4 class="main-title"><%= link_to "Simple Airbnb", flats_path, :class => "home-btn" %></h4>
  <button class="btn btn-outline-primary" data-action="click->show-form#show">New Flat</button>
  </div>
  <div class="form-container">
  <div class="form d-none" data-show-form-target="form">
    <h5>New Flat</h5>
    <%= simple_form_for(@flat) do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :number_of_guests %>
    <%= f.input :price_per_night %>
    <%= f.submit %>
    <% end %>
  </div>
  </div>
</div>
    <%= yield %>
  </body>
</html>
